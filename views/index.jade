html
  head
    link(rel='stylesheet' href='styles.css')
  body
    h1 #{greeting}
    h2 Request count: #{requestCount}
    ul
      each song in songs
        li #{song.title} (#{song.author})

    form(action="/logout" method="post")
      button Log Out